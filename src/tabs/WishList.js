import {View, Text} from 'react-native';
import React from 'react';

const WishList = () => {
  return (
    <View style={{flex: 1, justifyContent: 'center', alignItems: 'center'}}>
      <Text style={{color: '#0d0c0c', fontSize: 18}}>WishList</Text>
    </View>
  );
};

export default WishList;
